- Python 3.12.X como lenguaje de programación
- FastAPI para montar la API del backend + Pydantic v2 para schemas y validación
- SQLAlchemy 2.0 como ORM + Alembic para migraciones
- Uvicorn como servidor web ASGI de desarrollo

- PostgreSQL como BD relacional (via Docker)

- uv como gestor de dependencias

- pytest para tests
